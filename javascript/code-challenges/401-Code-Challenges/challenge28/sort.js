'use strict';

const { mainModule } = require('process');
const { Movies } = require('./movies.js');


function sortYear(arr) {
  arr.sort(callback)
}

function compareYear() {
  //do the thing
}


module.exports = {sortYear, compareYear}
